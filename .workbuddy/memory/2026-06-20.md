# 2026-06-20 工作日志

## 上游同步 dev → dev-web
- **源**: upstream `ArvinLovegood/go-stock` dev 分支
- **目标**: `dev-web` 分支
- **同步方式**: cherry-pick（跳过 Wails/CI/构建文件，仅取后端 Go 代码 + 前端 Vue 组件改动）

### 已同步的 5 个提交（从旧到新）：
1. `0c8787b` — 概念资金流向模块（208 行 concept_fund_flow_api.go + 650 行 conceptFundFlowChart.vue）
2. `2c96c4f` — 通达信 MAC 数据源优先级（A 股 fallback 链）
3. `00600e3` — 通达信板块查询 + 量比计算
4. `3b4ca20` — 上海时区修复
5. `e95f73a` — macOS 构建脚本 + 依赖更新（仅后端测试文件 + go.mod）

### 额外修复
- `fe65984` — 修复上游 `models.go` 中 PageSize/TotalPages 重复 json tag 问题

### 跳过的 4 个提交
- 4 个 Windows 卸载器/CI 优化（仅 `.github/workflows/` + `build/windows/installer/`）

### 编译验证
- `go build ./backend/...` ✅ 通过
- `go vet` 仅有上游预存在的 `stock_data_api_test.go:IsHs` 警告（不影响功能）
