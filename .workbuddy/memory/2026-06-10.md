# 2026-06-10 工作记录

- 修复钉钉通知功能 P1（用户隔离配置读取）：`dingding_api.go` 新增 `SendToDingDingWithConfig`/`SendDingDingMessageWithConfig`，接受 `enabled`/`robotURL` 参数，不再依赖全局 `GetSettingConfig()`；`web_handler.go` 的 `SendDingDingMessage` 改为 `GetSettingConfigByUserID(userID)` 读取当前用户配置；Web端消息标题改为「股海智擎」前缀。
- 修复钉钉通知功能 P2（SendDingDingMessageByType Web fallback）：`wails-bridge.js` 补充实际 API 调用 `/dingding/message`，替换原来的 `Promise.resolve('发送成功')` 空实现；根据 `msgType` 映射报警类型名称（涨跌/股价/成本价报警）作为标题。
- 提交 `6f75b6a`。

- 修复 `PUT /api/v1/cron-task/undefined` 问题：`wails-bridge.js` 的 `UpdateCronTask` fallback 用 `arg1.ID`（大写）构造 URL，但前端 `formData.id` 为小写，导致 `arg1.ID = undefined`。改为 `arg1.ID || arg1.id` 兼容。提交 `4693213`。

- 修复定时任务"未来5次执行时间"Web端为空：`wails-bridge.js` 的 `CalculateNextRunTimes`/`CalculateNextRunTime` fallback 为空实现（`Promise.resolve([])`/`Promise.resolve('')`），后端也缺少 HTTP handler。新增 `web_handler.go` handler + `server.go` 路由 `/cron-task/calculate-next-runs` 和 `/cron-task/calculate-next-run`，`wails-bridge.js` 补充实际 API 调用。提交 `72e65cc`。

- 修复编辑定时任务时任务参数被覆盖为空：`handleSubmit` 中 `formData.params = generatedParamsJson.value` 无条件覆盖，但 `generatedParamsJson` 只处理 `stock_analysis`/`market_analysis`，其他类型返回 `undefined` 导致 textarea 中用户填写的参数被清空。改为仅当 `generatedParamsJson` 有值时才覆盖。提交 `27ac7a3`。

- 修复定时任务不按cron执行：Web端 `Scheduler` 只有硬编码的市场统计/热词/板块资金3个任务，没有像桌面端 `App.InitCronTasks()` 从 `cron_tasks` 表加载用户自定义定时任务。集成 `robfig/cron` 调度器到 `scheduler.go`，新增 `initCronTasks()`/`AddCronTask()`/`RemoveCronTask()`；`web_handler.go` 创建/更新/删除/启用禁用后同步调度器；用 `handlers.CronSchedulerHooks` 回调模式解决循环导入。提交 `fb871cd`。

- 修复 `GET /api/v1/custom-strategies` 返回 404：前端 `wails-bridge.js` 已有 Web fallback 调用，但后端缺少路由和 handler。新增 4 个 handler + 4 条路由 + `MigrateAllTables` 添加 `CustomStrategy` 表迁移。提交 `35167d7`。
