# 2026-06-06 工作日志

## Web 端 Agent Chat 配置查询用户隔离修复
- **问题**：`AgentChat` handler 和 `AITradeAnalyze` handler 调用 `agent.ChatWithContext` 时，Agent 内部用 `GetSettingConfig()` 查询的是 `user_id=0` 的全局配置，导致 Web 端登录用户查不到自己的 AI 配置
- **修复**：
  - `agent_api.go`：`ChatWithContext` 和 `newStockAiAgent` 签名保持不变（桌面端不受影响），通过 `optsOverride[2]` 传递 `userID` 字符串
  - `newStockAiAgent`：从 `optsOverride[2]` 解析 userID，`userID>0` 时用 `GetSettingConfigByUserID(userID)` 查配置，`userID=0` 时用 `GetSettingConfig()`（桌面端行为不变）
  - `ChatWithContext` 中第二个 `GetSettingConfig` 调用也改为同样的逻辑
  - `ai_handler.go`：`AgentChat` 和 `AITradeAnalyze` 都在调用 `ChatWithContext` 时传入 `"", "", userIDStr"` 作为 optsOverride
  - 新增 `strconv` import
- **编译**：`go build ./web/...` ✅

## Web 端代码审查批量修复
- **1. AuthRequired 中间件**：`server.go` protected 组从 `AuthOptional()` 改为 `AuthRequired()`，所有业务接口必须登录
- **2. 路由整理**：`AgentChat`、`GetAIConfigs` 从 public 移到 protected；去掉 public/protected 重复路由（GetStockRealTime、SearchStocks、GetStockKLine）；public 组只保留 health
- **3. addStockFollowData 提取共享**：新建 `backend/data/stock_follow_helper.go`，`app.go` 和 `stock_handler.go` 都改为调用 `data.AddStockFollowData()`，修复 Web 端缺少 `Groups` 字段的 bug；`app.go` 保留价格更新逻辑（桌面端特有）
- **4. 统一 userID 获取**：6 个 handler 文件中 27 处 `if !exists { return 500 }` 全部删除（AuthRequired 保证 userID 必定存在），统一为 `userID, _ := ...`
- **编译**：`go build ./web/...` ✅

## GlobalStockIndexes panic 修复
- **问题**：`/api/v1/market/global-indexes` 返回 500，`market_news_api.go` 中 `res["data"].(map[string]any)` 裸断言 panic
- **修复**：改为安全断言 `if data, ok := res["data"].(map[string]any); ok`；handler 层也加 nil 防御
- **编译**：`go build ./web/...` ✅

## 合并上游 dev 分支 (54816df) 到 dev-web
- **上游 22 个 commit**：SMC指标/K线分析/雪球热股重试/财联社电报修复/基金优化/AI代理兼容性/自动更新增强/ARM64支持
- **9 个冲突文件**：agent_api.go、chat_model_factory.go、7个前端Vue/JS
- **冲突解决策略**：
  - `agent_api.go`：保留 Web 端 userID 隔离逻辑（optsOverride 提取 userID + GetSettingConfigByUserID），合并上游的 sysPrompt 强制规则
  - `chat_model_factory.go`：保留 `createHTTPClientWithProxy`（Web 端 SSE 流式需要），补 `crypto/tls` import
  - 前端 7 个文件：全部取上游版本（SMC/K线/基金等新功能）
- **编译**：`go build ./web/...` ✅

## wails-bridge.js 缺失导出修复
- **问题**：Web 模式下前端 `SyntaxError: The requested module does not provide an export named 'RestartAsAdmin'`
- **根因**：上游合并后新增的组件（kline-analysis.vue、FundKlineChart.vue 等）依赖了新函数，wails-bridge.js 缺失导出
- **修复**：补全 wails-bridge.js 缺失的所有导出：
  - `RestartAsAdmin` — 桌面端专用，Web 模式 no-op（`Promise.resolve()`）
  - `GetFollowedFundPaged` / `SearchFundCodes` — 新增基金分页和搜索 API 桥接
  - `GetCustomStrategyList` — 新增策略列表分页 API 桥接
  - `GetChipDistribution` — 新增筹码分布 API 桥接
  - 通达信系列（GetTdxCallAuction 等 6 个）— 新增 API 桥接 stub
  - `HideToTray` / `ShowFromTray` — 桌面端托盘，Web no-op
- **编译**：前端 build ✅，Go build ✅

## Web 端菜单布局重构：底部 Tab → 顶部导航
- **问题**：底部水平 n-menu 在 Web 端和手机端不美观，操作区域远、遮挡内容、子菜单需额外 Drawer
- **方案**：顶部 Tab + 二级标签页（同花顺/雪球风格），用户选择方案A
- **实现**：
  - `WebLayout.vue`：完全重写，顶部导航栏（go-stock 品牌 + 市场状态 + n-tabs 主Tab）+ 二级标签行
  - `App.vue`：检测 Web 模式（`useIsWebMode`），Web 模式用 WebLayout 替代底部 n-menu；桌面端 Wails 保持不变
  - 主Tab 固定 7 个：自选/行情/K线/基金/AI/研究/设置
  - 二级标签精确定义（不从 menuOptions.children 提取避免重复）
  - 市场状态只显示在 brand-status 小标签中，不出现在 Tab 标签
  - 移动端只显示 4 个主Tab + "更多" Drawer
  - `require()` 改为 ES `import`（EventsEmit 顶部导入）
- **编译**：前端 build ✅

## wails-bridge.js API 路径修复（public→protected迁移后前端未同步）
- `/public/ai/configs` → `/ai/configs`
- `/public/ai/agent-chat` → `/ai/agent-chat`（SSE URL）
- `/public/stocks/:code/kline` → `/stocks/:code/kline`（2处）
- GetUplimitHot: 裸 `fetch` 改 `apiService.client.get`（带 auth）；返回值从 `res.data?.data` 改 `res.data`（保留 code 字段）

## Agent Chat 错误提示修复
- **问题**：Agent 调用失败（如 max_tokens 无效 400 错误）时前端无限转圈
- **修复**：
  - `wails-bridge.js` ChatWithAgent SSE：HTTP 非 200 时读取响应体提取错误信息，发送 `{error}` 事件；网络错误和 SSE 读取错误同理
  - `agent-chat.vue` handleAgentMessage：检测 `data.error`，停止 loading + isStreamLoad，在聊天消息中显示错误内容