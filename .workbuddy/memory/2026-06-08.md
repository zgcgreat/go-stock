# 2026-06-08 工作日志

- 修复移动端二级菜单横向滑动：为 `market.vue` 和 `fund.vue` 的内部 NaiveUI `n-tabs` 补充移动端横向滚动样式；确认 `researchIndex.vue` 已有对应样式。前端构建 `npm --prefix frontend run build` 通过。
- 审查 Web 端二次开发最小改动原则：结论是后端 Web 层隔离相对较好，但前端仍有较多原桌面组件直接改动和 18 个文件直接 import `wailsjs`，`App.vue` 混入 Web 分支较重，建议逐步统一到 `services/wails-bridge.js` 并将 Web 壳组件独立化，降低上游合并冲突。
- 逐一修复前端直接 `wailsjs` 引用：将 `App.vue`、`stock.vue`、基金/研究/设置/提示词/技能/MCP/K线等组件的 Wails API/runtime import 统一迁移到 `frontend/src/services/wails-bridge.js`；`settings.vue` 去除对 `wailsjs/go/models` 构造类依赖，改用普通对象。复查 `frontend/src` 中仅剩注释包含 `wailsjs`，无直接导入；前端构建通过。
- 继续优化 Web 二次开发隔离：新增 `frontend/src/components/layout/WebAppShell.vue` 抽离 Web 顶部导航/登录页壳层，`App.vue` 只保留薄 Web 分支；`WebLayout.vue` 去除对桌面 `menuOptions/marketStatus/select` 的耦合；新增 `internal/migrations/web_migrations.go`，将 Web SQLite 兼容迁移从 `internal/webserver/server.go` 抽出。验证：`npm --prefix frontend run build` 与 `go build ./web` 均通过。
- 完成 `wails-bridge.js` Web fallback 覆盖补强：导出文件能力在 Web 端走浏览器下载；交易统计/市场指数/情绪分析/批量删除 AI 结果/AddCronTask 等补齐 HTTP 或组合 fallback；技能管理补齐创建、更新、删除、启用、分类/启用筛选；MCP 管理补齐列表、详情、创建、更新、删除、启用、测试、工具查询等 Web API，并在 `internal/webserver/server.go`/`internal/handlers/web_handler.go` 注册对应后端路由和 Handler。验证：前端构建、`go build ./web`、直接 `wailsjs` import 检查均通过。
- 已提交 Web 隔离与桥接层优化代码，提交号 `abcf862`，提交信息 `refactor: improve web bridge isolation`。提交前验证：`npm --prefix frontend run build`、`go build ./web`、直接 `wailsjs` import 检查、`git diff --check` 均通过；敏感字段检查仅命中空配置字段名/空值。
- 修复 Web 端旧 SQLite 保存配置报 `settings has no column named http_proxy`：在 `internal/migrations/web_migrations.go` 的 settings 兼容迁移中补齐 `http_proxy`、`http_proxy_enabled`、`enable_agent`，并新增 `ai_config` 缺列补丁（`http_proxy`、`http_proxy_enabled`、`session_id`、`thinking`）。验证 `go build ./web` 通过；已提交 `79087dc fix: migrate missing settings proxy columns`。
- 分析移动端访问最小改动适配方案：当前已有 WebLayout 顶部导航、一级/二级 Tab 横向滚动与 useResponsive；主要缺口是 WebAppShell 固定 `100vh - 80px`、全局缺少移动端表格/弹窗/表单兜底、部分浮动搜索框固定 400px、少数页面硬编码宽度。建议优先只改 Web 壳层和全局 CSS，再对 stock/FundFollow 等关键浮动搜索框做少量 scoped 覆盖。
- 确定移动端访问最佳改造方案：采用“Web 外壳优先 + 全局 CSS 兜底 + 少量高频组件补丁”的三层渐进方案，避免重构桌面组件和逐页大改；优先修改 `WebAppShell.vue`、`WebLayout.vue`、`style.css`，再处理 `stock.vue`、`FundFollow.vue` 等固定宽度溢出点，最后按 320/768/1024px 做验证。
- 已按最小改动方案实施移动端适配：`WebAppShell.vue`/`WebLayout.vue` 改用 `100dvh` 和独立滚动容器；`Login.vue`/`Register.vue` 支持小屏宽度；`style.css` 增加 NaiveUI 表格、弹窗、表单、Tabs 的移动端兜底；`stock.vue`/`FundFollow.vue` 将固定 400px 底部搜索框改为响应式类。验证 `npm --prefix frontend run build` 和 `git diff --check` 通过。
- 审查移动端适配改动：业务代码 7 个文件、约 138 行变更，整体符合“Web 壳层 + 全局兜底 + 关键组件补丁”的最小改动原则；`git diff --check` 通过。审查发现无阻塞问题，建议后续人工在 320/375/414/768px 真机或浏览器设备模式下重点验证登录页、顶部导航、自选/基金底部搜索框、表格横滚和弹窗宽度。
- 修复线上 `/api/v1/config` 保存配置报 `settings has no column named http_proxy`：确认根因是 Web 初始化迁移中同时 AutoMigrate 了 `data.Settings{}` 与同表名的旧模型 `models.OldSettings{}`，首次部署建表可能被旧模型覆盖/回退导致新字段缺失。按用户要求改为初始化阶段解决：移除 `internal/webserver/server.go` 中 `MigrateAllTables()` 对 `models.OldSettings{}` 的 AutoMigrate，确保新库首次创建 `settings` 表只使用 `data.Settings{}`，直接包含 `http_proxy/http_proxy_enabled/enable_agent` 等字段；取消保存配置前迁移兜底。验证 `go build ./web` 与 `git diff --check` 通过。
- 已提交移动端适配与 settings 初始化修复，提交号 `2718e44`，提交信息 `fix: improve mobile web layout and settings init`。提交包含 8 个业务文件：移动端布局/全局样式/底部搜索框响应式，以及移除 Web 初始化中对 `models.OldSettings{}` 的同表迁移。提交前验证：`npm --prefix frontend run build`、`go build ./web`、`git diff --check`、敏感字段检查均通过。
- 更新 Windows 托盘显示文案：`app_windows.go` 中 systray 标题由 `go-stock` 改为 `股海智擎`，tooltip 改为 `股海智擎：AI驱动的智能投研决策中枢`，并已执行 gofmt。
- 确认 `ai-assistant-web/static` 是构建产物并从版本控制中清理：在根 `.gitignore` 增加 `ai-assistant-web/static/`，执行 `git rm -r --cached --ignore-unmatch ai-assistant-web/static` 仅移除 Git 索引、不删除本地文件；验证 `git check-ignore` 命中该目录，后续新构建产物不会再作为未跟踪文件出现。已提交 `4146182 chore: ignore ai assistant static build output`，包含 static 构建产物从索引删除、`.gitignore` 更新，以及托盘标题文案更新。
- 修复提示词广场 Web 端不应展示外部广场默认用户：`promptPlaza.vue` 中发布按钮旁边的用户标签和退出按钮仅桌面端显示，Web 端保留后端自动登录逻辑但不展示 Joy/VIP/退出入口。补强手机二级菜单横滑：`style.css` 在移动端对 `.n-tabs-nav-scroll-content` 强制 nowrap/max-content，并让 `.n-tab` 不收缩，修复只给 wrapper 加 overflow 但内容被压缩导致无法滑动的问题。验证 `npm --prefix frontend run build` 与 `git diff --check` 通过。
- 同步移除问答广场 Web 端默认登录信息展示：`promptQa.vue` 中“提问”按钮旁边的已登录用户标签/未登录提示改为仅桌面端显示，Web 端不再展示后台自动登录的外部广场用户；验证 `npm --prefix frontend run build` 和 `git diff --check` 通过。
- 已提交广场用户展示隐藏、移动端二级菜单横滑补强和产品标题文案更新，提交号 `6de0519`，提交信息 `fix: hide plaza web user badges`。提交包含 `promptPlaza.vue`、`promptQa.vue`、`style.css`、`frontend/index.html`、`main.go`；提交前验证 `npm --prefix frontend run build`、`go build .`、`git diff --cached --check` 和敏感字段检查通过。
- 已将上游开源 `origin/dev` 最新代码合并到 `dev-web` 并适配 Web 端，提交号 `3fd2144`，提交信息 `merge: sync upstream dev into dev-web`。上游新增“板块资金流向”功能；Web 端适配包括：`bkFundFlowChart.vue` 改走 `wails-bridge.js`、新增 `/api/v1/market/bk-fund-flow/{fetch,top,list}` 路由和 Handler、Web 初始化迁移加入 `BKFundFlow`、Web scheduler 增加交易时段每分钟采集、wails-bridge 补齐 fallback。合并冲突文件：`backend/models/models.go` 保留提示词广场缓存模型并追加 BKFundFlow 模型，`market.vue` 同时保留 MarketStatistic 和 BKFundFlowChart。验证 `npm --prefix frontend run build`、`go build ./web`、`go build .`、`git diff --cached --check`、敏感字段检查通过。
