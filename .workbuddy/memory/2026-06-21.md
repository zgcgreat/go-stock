# 2026-06-21 工作日志

## 修复 conceptFundFlowChart.vue 构建失败

### 问题
上游同步引入的 `conceptFundFlowChart.vue` 从 `wailsjs/go/main/App` 导入 3 个函数，但 Web 模式无此路径。

### 修复（4 个文件修改）
1. **`internal/handlers/web_handler.go`** — 新增 3 个 Handler：
   - `GetAllConceptCodes`：获取所有概念代码/名称
   - `GetConceptFundFlowListByDate`：获取概念指定日期资金流向
   - `GetConceptFundFlowTopListByDate`：获取指定日期概念资金排名
2. **`internal/webserver/server.go`** — 注册概念资金流向路由 (`/market/concept-fund-flow/*`) + AutoMigrate 添加 ConceptFundFlow 模型
3. **`frontend/src/services/wails-bridge.js`** — 添加 3 个桥接函数
4. **`frontend/src/components/conceptFundFlowChart.vue`** — import 路径改为 `../services/wails-bridge.js`

### 验证
- `go build ./internal/...` ✅ — 通过
- `go build ./backend/...` ✅ — 通过
- `npm run build` ✅ — 通过（1m 3s，36760 modules）

## 修复登录页触发大量业务 API 请求

### 问题
Vue Router 守卫异步执行期间，默认路由 `/` 的 stock.vue 已被挂载，
onBeforeMount/onMounted 立即触发 7+N 个 API 请求。30+ 组件都有此问题。

### 修复：`frontend/src/services/api.js`
在 axios 请求拦截器中统一拦截：Web 模式下无 token 时，
非公开路由请求直接短路返回 `axios.Cancel`，不发 HTTP 请求。
- 公开 API（`/public/`、`/auth/`）不受影响
- 所有组件无需单独加 token 检查，一层拦截全部解决
- 原先在 stock.vue 和 WebLayout.vue 加的单独 token 检查已撤回

## AI 菜单审查 + 三项修复

### 分析结论
AI 菜单是**上游原有功能**（非 Web 端新增），但 Web 端实现有 3 个问题。

### P0: agentMode 参数丢失 (BUG)
- **根因**: `wails-bridge.js` ChatWithAgent 只映射 6 参数到 JSON body，第 7 参数 agentMode 被丢弃
- **影响**: 用户选择"快速"/"规划"模式实际走 auto，UI 选了白选
- **修复**:
  1. `wails-bridge.js` — 加 arg7 参数 + body.agentMode 字段
  2. `internal/handlers/ai_handler.go` — AgentChat req struct 加 AgentMode 字段，传给 ChatWithContext

### P1: enableAgent 配置控制缺失
- **根因**: Web 端 AI 菜单始终显示，无视 enableAgent 配置
- **修复**: `WebLayout.vue` — 读取 GetConfig().enableAgent，控制 AI 菜单可见性

### P2: 会话持久化
- **根因**: agent-chat.vue 对话刷新即丢失，后端已有 session API 但前端未接入
- **修复**: `agent-chat.vue` — 接入 SaveAiAssistantSession/GetAiAssistantSession：
  - onBeforeMount 恢复上次会话
  - onBeforeUnmount 保存当前会话
  - 对话完成后自动保存

### 验证
- `go build ./internal/...` ✅
- `npm run build` ✅
