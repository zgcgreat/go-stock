# 2026-06-12 工作日志

- `/api/v1/market/hot-stock` 接口查不到数据根因分析：该接口调用 `XUEQIUHotStock()`，依赖 chromedp（Chrome/Chromium 浏览器）通过 headless 模式访问雪球首页获取 cookie。Dockerfile.backend 基于 Alpine Linux 只安装了 ca-certificates + tzdata，没有 Chromium 和字体/NSS 库，导致 `CheckBrowser()` 返回空 → chromedp 启动失败 → cookie 为空 → 被雪球拒绝返回空数组。本地 Windows 因自动检测到 Edge/Chrome 而工作正常。
- 修复 Dockerfile.backend：安装 chromium + fontconfig + freetype + nss + libX11 + libXcomposite + libXdamage + libXrandr + libxkbcommon + mesa-gl + mesa-gles + alsa-lib + cups-libs + dbus-libs。提交 `69ec1ac`。
- 影响范围：/market/hot-stock、/hot-event、/hot-topic、eastmoney K线爬虫等所有依赖 chromedp 的接口均可在 VPS Docker 部署后正常工作。
- 检查上游 dev 分支更新（2026-06-11）：无新提交，最新仍为 ef9797b。
