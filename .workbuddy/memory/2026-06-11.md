# 2026-06-11 工作日志

- 检查上游 dev 分支更新：无新提交，最新仍为 ef9797b。
- 分析 VPS 定时任务不执行问题：根因是 Web 端 `scheduler.go` 的 `initCronTasks()` 在 `Start()` 时被调用，从数据库加载所有 `enable=true` 的 cron_tasks 注册到 robfig/cron 调度器。但 VPS 部署时需确认：1) 数据库中 cron_tasks 表是否有数据；2) `GO_STOCK_DB_PATH` 环境变量是否正确指向 SQLite 文件；3) Docker 容器日志中是否有 `[Scheduler] 注册定时任务` 输出；4) 桌面端 `App.InitCronTasks()` 会自动创建 `stock_change_save` 默认任务，Web 端 `scheduler.initCronTasks()` 不会创建默认任务。需补齐 Web 端默认任务创建逻辑。
